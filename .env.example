# Optional: Environment configuration for development

# CoinGecko API (no key required for basic endpoints)
COINGECKO_API_URL=https://api.coingecko.com/api/v3

# Backend settings
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend settings
REACT_APP_API_URL=http://localhost:8000/api

# Optional: Redis for caching (not required for basic functionality)
# REDIS_URL=redis://localhost:6379

# Optional: Database settings (currently using in-memory storage)
# DATABASE_URL=sqlite:///./crypto.db